#include <stdio.h>
#include "main.h"
/**
 * reset_to_98 -  takes a pointer to an int
 * @n : is a pointer
 * Return Always 0 (Success)
 */
void reset_to_98(int *n)
{
		int n;
		int *bat;

		n = 98;
		n = &bat;
		printf("%p\n", n);
		return (0);
}
