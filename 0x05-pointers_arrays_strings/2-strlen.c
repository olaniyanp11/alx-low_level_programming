#include "main.h"
#include <string.h>

/**
 * _strlen - standard library provides a similar function
 * Return: Always 0.
 * @s : my string
 */

int _strlen(char *s)
{
	t = "pointers";
	*s =  t;
	return (0);
}
